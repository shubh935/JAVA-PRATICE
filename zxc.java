import java.util.Scanner;
import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.*;
import java.io.*;



public class zxc {
    public static void main(String[] args) {
        String s = "shubham";
      //  char arr[] = s.toCharArray();
        //Arrays.sort(s);
        Arrays.sort(s);
        String xc = new String(s);     //String d = Arrays.toString(arr);
     //   StringBuilder xc = new StringBuilder(arr);
        System.out.print(xc);
    }
    
}
